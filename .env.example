# Copy to .env.local for local dev, or set these in Vercel Project Settings -> Environment Variables

# Supabase (required for Wish feature)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Ollama
# Dev default uses the Vite proxy at /ollama -> http://localhost:11434
# In production (Vercel), you cannot reach your laptop's Ollama from the browser.
# Set a hosted Ollama-compatible endpoint if you have one.
VITE_OLLAMA_URL=
VITE_OLLAMA_MODEL=llama3.2:3b

# Toolhouse (recommended for production)
# In production on Vercel, Toolhouse runs via a serverless proxy so keys stay server-side.
# Set these as Vercel Environment Variables (NO VITE_ prefix):
TOOLHOUSE_URL=
TOOLHOUSE_API_KEY=
TOOLHOUSE_MODEL=

# Toolhouse Agent (bounty mode: Toolhouse↔Supabase linker)
TOOLHOUSE_AGENT_URL=
TOOLHOUSE_AGENT_API_KEY=

# Optional: direct Toolhouse from the browser (dev only; NOT recommended for production)
VITE_TOOLHOUSE_URL=
VITE_TOOLHOUSE_API_KEY=
VITE_TOOLHOUSE_MODEL=

# Optional telemetry endpoint (leave blank to keep telemetry local-only)
VITE_TELEMETRY_ENDPOINT=
